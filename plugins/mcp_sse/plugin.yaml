name: mcp_sse_plugin
version: 0.0.1
display_name: MCP SSE Plugin
description: openJiuwen 插件，通过 HTTP with SSE 或 Streamable HTTP 使用 MCP 协议发现和调用工具。Fetch and call tools by MCP protocol via HTTP with SSE or Streamable HTTP.
runtime:
  type: python-package
metadata:
  author: openJiuwen
  tags: [mcp, sse, utilities]
compatibility:
  python: ">=3.11,<3.14"
  agent_core: ">=0.1.5,<0.2.0"
