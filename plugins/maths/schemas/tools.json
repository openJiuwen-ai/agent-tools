{
  "tools": [
    {
      "name": "eval_expression",
      "description": "计算数学表达式的结果（本地使用 NumExpr）。",
      "input_params": {
        "type": "object",
        "properties": {
          "expression": {
            "type": "string",
            "description": "要计算的数学表达式，例如：cos(60)、1+(2+3)*4"
          }
        },
        "required": [
          "expression"
        ]
      }
    }
  ]
}
